Pages where a <code><nowiki>{{Value}}</nowiki></code> names an item, field or format the pack data does not have ([[Template:Value]]). The build fails while any page is here.__HIDDENCAT__
__EXPECTUNUSEDCATEGORY__
